/*
 * Trival Settings should store settings that have little
 * connections with business logic, such as window position, text field value etc.
*/

import Qt.labs.settings as Lab

Lab.Settings {
    category: "trivial"
}
